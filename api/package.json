{
  "name": "word-art-serverless",
  "version": "0.2.0",
  "private": true,
  "description": "AWS Lambda service that renders and stores Word Art SVGs",
  "scripts": {
    "package": "serverless package",
    "test": "python -m pytest"
  },
  "engines": {
    "node": ">=18.17.0"
  },
  "devDependencies": {
    "serverless": "4.40.0"
  }
}
